# Hero Redesign Prototype — Fix Plan

**Scope:** Edit ONLY `hero-redesign-preview.html` (throwaway prototype). Do NOT touch `index.html`, `css/index.css`, `js/main.js`.

## Diagnosis (from live Playwright render, viewport 1036×703)
- `.hero__logo`: top 232 / bottom 382 → vertical **middle**, NOT the bottom-right corner. Root cause: it is `position:absolute` inside `.hero__half--right` (top 290→bottom 414), which is itself vertically centered in the hero. The `logoSettle` animation ends relative to that centered box.
- `.hero__scroll`: top 306 / left 582 → **center of the page**. Same root cause (anchored to the centered right-half, not the hero).
- `.hero__title`: opacity 1 but shows **hallucinated** copy "We Craft ● Changes That Move People".
- `#intro`: shows **hallucinated** "ACT / Creative Studio".
- Video: Coverr + Pexels `<source>`s load fine for the user (403 is only the sandbox blocking hotlinks). No beach poster (already removed).

## Real content (from production `index.html`)
- Hero/footer vertical type = `Action` / *Changes* / `Things.` with red dot (`index.html:657`, mirrors `.footer-left h1`).
- Real pre-load intro = ACT logo + `A.C.T.` + `Action Changes Things.` (`index.html:34-50`).

## Changes
1. **Reposition logo + scroll** — Move `.hero__logo` and `.hero__scroll` out of `.hero__half--right` to become direct children of `.hero` (`position:relative`), so they anchor to the hero box:
   - Logo → bottom-right corner (`right/bottom: var(--space-6)`); `logoSettle` keyframe ends at the corner.
   - Scroll → bottom-left corner (`left/bottom: var(--space-6)`). Fixes "middle of the page".
2. **Fix hero copy** — Left half = footer-mirroring vertical typography: small eyebrow `Project A.C.T. — Digital Archive`, then `<h1>` `Action` / `<em>Changes</em>` / `Things<span class="dot">.</span>` (Playfair 700/900, line-height ~0.94, red dot). Remove invented "Cinematic Storytelling" eyebrow, "We Craft Changes That Move People" title, "A multidisciplinary studio…" caption, and fake CTAs.
3. **Fix intro copy** — `#intro` shows real ACT intro: `act-logo.png` + logotype `A.C.T.`, then `Action Changes Things.` (no "Creative Studio"). Keep pre-load animation, `body.ready` reveal gate, reduced-motion skip.
4. **Keep video sources** — Coverr + Pexels only; no poster image.

## Verify
- Re-serve `py -m http.server 8123`; Playwright `evaluate` asserts:
  - `.hero__logo` bottom ≈ viewport bottom (corner), `.hero__scroll` left ≈ small inset / bottom-left.
  - `.hero__title` text = `Action Changes Things.`
  - `#intro` not present after ~2.5s (or `display:none` under reduced-motion).
- Console should show only the favicon 404.
